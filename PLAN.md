# 项目规划

> 本文档是项目的最高层规划，记录目标定义与阶段路线。所有后续设计争议以此为裁决依据。
> 状态：目标已确认（2026-07）。流程与功能范围讨论中。

## 一句话定位

**让几十人规模、没有职业中层的公司，靠 AI 运转起来。**

AI 是每个员工的直属经理 + 老板的参谋部；IM、Web 都只是它的接口。

## 终极目标（北极星）

一家公司接入后，管理的完整闭环由 AI 驱动：

- **老板**只做两件事：下达目标、做 AI 递上来的决策。不再追问进度、不再传话、不再自己汇总。
- **员工**只面对一个对话入口：接任务、问上下文、汇报进度、被提醒被催办。不填表、不写周报、不开对齐会。
- **系统**随使用时间增值：对每个人的了解越来越准（画像），对公司历史的记忆越来越厚（知识），新项目的拆解分配越来越聪明。

**达成标准：把这套系统关掉一周，公司管理会瘫痪。**

## 阶段目标

### 目标一：我们公司离不开它

第一个里程碑，也是产品成立的证明。

- 公司所有项目和任务 100% 在系统内流转，没有旁路（不再用群消息派活）
- 老板每天通过 AI 摘要掌握全局，主动追问进度的次数趋近于零
- 催办、到期提醒、每日待办、每周汇总全部由 AI 主动完成
- 量化标准：老板每周花在「了解进展 + 传达催办」上的时间下降一半以上

### 目标二：别的公司愿意用它

- 2~3 家外部公司真实部署、持续使用超过一个季度
- 部署门槛：客户拿到二进制 + 数据库 + API key，半天内跑起来
- 新公司冷启动当天就有价值（任务流转 + AI 主动运营不依赖历史积累）

### 目标三：它成为一个生意（远期）

- 私有部署授权/订阅模式跑通，客户自带 API key
- 暂不展开，不影响当前设计

## 非目标（明确不做）

- **不做 IM**：不做聊天、群组、通讯录同步。IM 是别人的，我们只做接口。
- **不做通用项目管理软件**：不跟 Jira/飞书比看板、甘特图、自定义工作流引擎。界面能力做到「够用」为止。
- **不做 HR 系统**：画像服务于任务分配和管理判断，不做考勤、薪酬、绩效打分。
- **不做大公司**：几百人、多层级、有职业管理层的公司不是目标客户，他们的需求会把产品带歪。
- **第一阶段不做多租户 SaaS**：一套部署服务一家公司。

## 第一性原则

后续所有规划与设计争议，以这三条裁决：

1. **AI 主动，人被动** — 凡是需要人记得去做的环节（看板、催人、写汇总），都是产品的失败；方向永远是 AI 主动推给人。
2. **接口皆可换，中枢不可换** — TG、Web、企微都是外设；价值全部沉淀在中枢（权限、任务、画像、知识）。
3. **越用越值钱** — 每个功能都要问：它有没有让系统积累别人拿不走的东西？

## 已定的方向性决策

| 决策 | 结论 | 理由 |
|------|------|------|
| 实现语言 | Go 重写 | 部署简单（单二进制）、稳定、高效 |
| 存储 | PostgreSQL（pgx + 内嵌迁移） | 任务树/权限/审计天然关系型；事务是刚需 |
| AI 引擎 | 自有 `ai.Engine` 接口，中枢走 eino 直调 API；CLI 只用于 nbco-worker 交互式 PTY | 中枢需要稳定 API 契约；工作机自动干活走人类同款 CLI 交互 |
| 工具抽象 | 自有 `ai.Tool`（JSON Schema + handler），eino/MCP/HTTP 各自薄适配 | 接口皆可换，中枢不可换 |
| 交付形态 | 私有部署优先，SaaS 后置 | 数据敏感（任务/画像/评价），信任门槛；Go 单二进制交付成本低 |
| AI 成本 | 客户自带 API key | 与私有部署配套，不承担用量风险 |
| 入口 | 可插拔接口层（IM / Web 均为外设） | 不绑死任何 IM；一个简单 Web 页面即可作为最小入口 |
| 第一个客户 | 我们自己的公司 | 真实需求驱动，先为一家做好，再为第二家泛化 |

## 待讨论

- [ ] 核心流程设计（目标下达 → 拆解分配 → 执行汇报 → 汇总决策 → 人与知识积累）
- [ ] 目标一的功能范围与里程碑拆分
- [ ] 权限模型升级（是否引入组/岗位）
- [x] 技术架构 —— Go 骨架已落地（见 README「架构」一节）：P1 完成 legacy 功能重建 + 无状态化 + 审计；流程讨论的结论在此骨架上迭代
